# nfs-ganesha-image

The `Containerfile` in this repo is based on, and uses sections copied from, the `ceph/ceph`
[`Containerfile`](https://github.com/ceph/ceph/blob/c743821fba4c7d4d10b2fc12facd975b6fd18c55/container/Containerfile).

The LICENSE for this repo is same as that of the `ceph/ceph` `Containerfile`, which ever LICENSE that might be.
